#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int main() {

    int n, m;

    printf("\nLength of first string array : ");
    scanf("%d", &n);
    printf("\nLength of second string array : ");
    scanf("%d", &m);


    /*

    char s[10][n];
    char t[10][m];

    for(int i = 0 ; i < 10 ; i++) {
        printf("S array - Enter String (%d) > ", i+1);
        for(int j = 0 ; j < n ; j++) {
        scanf("%c", &s[i][j]);
        }
    }


    for(int i = 0 ; i < 10 ; i++) {
        for(int j = 0 ; j < n ; j++) {
            printf("String %d : %s", i, s[i][j]);
        }
    }

    */






    return 0;

}
